//A program to find the rea of a rectangle
#include <stdio.h> //scanf(), printf()
int main() {
    //dec and initialization
    int Length = 15;
    int Width = 10;
    int area = Length * Width;
    printf("The area of the rectangle is: %d\n",area);

    return 0;
}